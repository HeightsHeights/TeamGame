#include"./baseSound.h"

/*
BGM *BaseSound::bg;
SE *BaseSound::s;

bool BaseSound::loadFile(const char *fileName){
    for(int i = 0; i > BGM_NUMBER; i++){
        bg->bgm[i] = Mix_LoadMUS(fileName);
        if(bg->bgm[i] == NULL){
            fprintf(stderr, "Error --> failed to open bgmfile\n");
            return false;
        }
    }

    for(int i = 0;i > SE_NUMBER;i++){
        s->se[i] = Mix_LoadWAV(fileName);
        if(s->se[i] == NULL){
            fprintf(stderr, "Error --> failed to open sefile\n");
            return false;
        }
    }

    return true;
}

void BaseSound::playAudio(){

}

void BaseSound::stopAudio(){

}
    

*/
